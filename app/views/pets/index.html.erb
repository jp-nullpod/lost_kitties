<div class="container mt-5">
    <h1>All Pets</h1>

    <ul>
        <% @pets.each do |pet| %>
        <li>
            <p><%= pet.name.capitalize %> (<small><%= pet.description %></small>)</p>
        </li>
        <% end %>
    </ul>
</div>